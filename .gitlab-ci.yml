image: docker:latest
services:
  - docker:dind
stages:
  - build

docker:
  stage: build
  artifacts:
    paths:
      - etc/
      - bin/
  script:
    - docker run -v "$(pwd):/src/" cdrx/pyinstaller-linux
    - mkdir -p etc/tmuxpy bin
    - cp *.conf etc/tmuxpy/
    - cp *.yml etc/tmuxpy/
    - cp dist/linux/* bin/
